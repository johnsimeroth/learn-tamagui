import { createAnimations } from '@tamagui/animations-css'

// TODO: make web animations bounce so they somewhat match native animations.

export const animations = createAnimations({
  fast: 'ease-out 0.05s',
  medium: 'ease-out 0.2s',
  slow: 'ease-out 0.3s',
})
